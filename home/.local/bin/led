#! /usr/bin/env bash
led=$(gum choose audio mic)
[ "$led" = audio ] && led=mute || led=micmute
state=$(gum choose on off)
[ "$state" = on ] && state=1 || state=0
echo "$state" | sudo tee /sys/class/leds/platform::"$led"/brightness >/dev/null
